export interface AboutContent {
  title: string;
  content: string;
  lastUpdated: string;
}

export interface ContactDetails {
  email: string;
  phone: string;
  address: string;
  whatsappLink: string;
  lastUpdated: string;
}

export interface AdminCredentials {
  email: string;
  password: string;
}